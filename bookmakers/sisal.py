# Questo file è stato creato per il bookmaker: Sisal
# Puoi aggiungere il codice specifico per il bookmaker Sisal qui.

import os
print(f'Il nome del file in esecuzione è: {os.path.basename(__file__)}')
