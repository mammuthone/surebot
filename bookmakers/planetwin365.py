# Questo file è stato creato per il bookmaker: Planetwin365
# Puoi aggiungere il codice specifico per il bookmaker Planetwin365 qui.

import os
print(f'Il nome del file in esecuzione è: {os.path.basename(__file__)}')
